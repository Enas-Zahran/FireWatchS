import 'package:flutter/material.dart';

class HeadCorrectiveLocationsPage extends StatelessWidget {
  const HeadCorrectiveLocationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}