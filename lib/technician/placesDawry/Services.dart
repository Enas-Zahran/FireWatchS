import 'package:flutter/material.dart';

class DawryServicesTech extends StatelessWidget {
  const DawryServicesTech({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
