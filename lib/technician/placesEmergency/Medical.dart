import 'package:flutter/material.dart';

class EmergencyMedicalTech extends StatelessWidget {
  const EmergencyMedicalTech({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
