import 'package:flutter/material.dart';

class EmergencyServicesTech extends StatelessWidget {
  const EmergencyServicesTech({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
