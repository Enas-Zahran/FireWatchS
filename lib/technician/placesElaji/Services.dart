import 'package:flutter/material.dart';

class ElajiServicesTech extends StatelessWidget {
  const ElajiServicesTech({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
