import 'package:flutter/material.dart';

class ManagerCorrectiveReports extends StatelessWidget {
  const ManagerCorrectiveReports({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}