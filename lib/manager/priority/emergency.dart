import 'package:flutter/material.dart';

class MangerEmergencyPage extends StatelessWidget {
  const MangerEmergencyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}