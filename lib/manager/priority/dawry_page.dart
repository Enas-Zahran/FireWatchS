import 'package:flutter/material.dart';

class ManagerDawriPage extends StatelessWidget {
  const ManagerDawriPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}